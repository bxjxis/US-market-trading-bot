ib_insync==0.9.86
pandas>=2.0.0
python-dotenv>=1.0.0
numpy>=1.26.0
pyarrow>=15.0.0

# Backtesting & optimisation
optuna>=3.6.0
sqlalchemy>=2.0.0

# Optional: interactive charts (equity curve + Optuna visualisations)
# pip install plotly
# plotly>=5.0.0

# Optional: static chart fallback when plotly is not available
# pip install matplotlib
# matplotlib>=3.7.0

# Optional: PostgreSQL driver (uncomment if using PostgreSQL instead of SQLite)
# psycopg2-binary>=2.9.0
